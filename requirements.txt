requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
browser-cookie3>=0.19.1
openpyxl>=3.1.0
pillow>=10.0.0
python-docx>=1.1.0

# 长图 JPG 导出需要额外安装（约 190 MB，含浏览器组件）：
#   pip install -r requirements-image.txt
#   python -m playwright install chromium
# 不安装时长图仍会生成 image_report/preview.html，只是没有 JPG。
